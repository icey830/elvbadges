deploy:
  provider: pages
    language: node_js
  skip_cleanup: true
  github_token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
  keep_history: true
install: npm ci
script:
  - npm start
  on:
    branch: master



